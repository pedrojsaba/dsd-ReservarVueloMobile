<h1>Listing seats</h1>

<table>
  <tr>
    <th>Id asiento</th>
    <th>Numero</th>
    <th>Posicion</th>
    <th>Flight</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @seats.each do |seat| %>
  <tr>
    <td><%= seat.id_asiento %></td>
    <td><%= seat.numero %></td>
    <td><%= seat.posicion %></td>
    <td><%= seat.flight_id %></td>
    <td><%= link_to 'Show', seat %></td>
    <td><%= link_to 'Edit', edit_seat_path(seat) %></td>
    <td><%= link_to 'Destroy', seat, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Seat', new_seat_path %>
